version: "1.0"
linter: jetbrains/qodana-python:2024.1
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses

# Añadir configuración del intérprete de Python
python:
  interpreter: /usr/local/bin/python  # Ajusta esto según la ubicación de tu intérprete de Python
